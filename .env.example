# OpenAI API key for baking your face into the soldier spritesheet.
# Copy this file to .env and paste your key (never commit .env).
# Bake uses gpt-image-1.5 (supports transparent PNG backgrounds).
OPENAI_API_KEY=sk-...
